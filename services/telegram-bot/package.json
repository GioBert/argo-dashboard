{
  "name": "@argo-dashboard/telegram-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --env-file=.env.local --watch src/app.js",
    "start": "node --env-file=.env.local src/app.js",
    "check": "tsc -p tsconfig.json --noEmit"
  }
}
